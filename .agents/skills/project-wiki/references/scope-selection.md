# Scope Selection

Use `project-wiki scope --project <path>` to preview default, Unity, and user exclusions. Automatically excluded Unity vendor folders are third-party boundaries. Review `Assets/Plugins/**` manually instead of auto-excluding it.
