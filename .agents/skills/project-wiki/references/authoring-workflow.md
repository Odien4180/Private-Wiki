# Authoring Workflow

Run init or update, review analysis scope, inspect `knowledge/document-plan.json`, classify systems/features, query related entities with `list`, `inspect`, and `context`, write Markdown in `AGENT` blocks, insert wiki links, add source captions, validate, then build.
