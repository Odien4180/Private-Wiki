# Update Workflow

After `project-wiki update`, use `tracking/updates.json` to locate affected entities and refresh only related `AGENT` blocks. Preserve manual content, unchanged documents, and report stale or orphan documents.
