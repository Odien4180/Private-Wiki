# Incremental Update (Planned - Milestone 4)

This document will define the `update` workflow: change detection
(git -> hash -> mtime), impact analysis over the knowledge graph,
incremental cross-link/caption updates, orphaned-document handling, and
recent-changes generation.

Not yet implemented. See `docs/architecture.md` for the milestone plan.
