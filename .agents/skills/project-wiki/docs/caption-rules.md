# Caption Rules (Planned - Milestone 3)

This document will define:

- footnote/caption syntax,
- Source Caption formatting, including the rule that line numbers must
  never be estimated by the agent (only reported when computed
  deterministically by the engine),
- backlink index generation (deterministic, not agent-authored).

Not yet implemented. See `docs/architecture.md` for the milestone plan.
