# Linking Rules (Planned - Milestone 3)

This document will define:

- the `[[Entity]]` / `[[Entity|Display Name]]` hyperlink syntax,
- the Full Cross-Link Pass workflow (run only after all entities are
  finalized),
- link density policy (first meaningful occurrence per section),
- the stop-list of common terms that are never auto-linked,
- ambiguous link handling and how candidates are recorded for validation.

Not yet implemented. See `docs/architecture.md` for the milestone plan.
